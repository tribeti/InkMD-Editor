<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="InkMD_Editor.Views.EditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:customcontrols="using:InkMD_Editor.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:InkMD_Editor.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:storage="using:Windows.Storage"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:vm="using:InkMD_Editor.ViewModels"
    NavigationCacheMode="Enabled"
    mc:Ignorable="d">

    <Page.Resources>
        <MenuFlyout x:Key="MyContextMenu">
            <MenuFlyoutItem Text="New Markdown File">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator Key="N" Modifiers="Control" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>
            <MenuFlyoutItem Text="New Folder">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator Key="N" Modifiers="Control,Shift" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>
            <MenuFlyoutItem Text="Paste">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator Key="V" Modifiers="Control" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>
            <MenuFlyoutItem Text="Delete">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Delete" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>
        </MenuFlyout>

        <DataTemplate x:Key="FolderTemplate" x:DataType="TreeViewNode">
            <StackPanel Orientation="Horizontal">
                <FontIcon Glyph="&#xED41;" />
                <TextBlock Margin="0,0,10,0" />
                <TextBlock Text="{x:Bind ((storage:StorageFolder)Content).DisplayName}" />
            </StackPanel>
        </DataTemplate>

        <!--  DataTemplate for files  -->
        <DataTemplate x:Key="FileTemplate" x:DataType="TreeViewNode">
            <StackPanel Orientation="Horizontal">
                <FontIcon Glyph="&#xE8A5;" />
                <TextBlock Margin="0,0,10,0" />
                <TextBlock Text="{x:Bind ((storage:StorageFile)Content).Name}" />
            </StackPanel>
        </DataTemplate>

        <!--  Template selector for ExplorerItem types  -->
        <local:ExplorerItemTemplateSelector
            x:Key="ExplorerItemTemplateSelector"
            FileTemplate="{StaticResource FileTemplate}"
            FolderTemplate="{StaticResource FolderTemplate}" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="0,0,12,0">
            <customcontrols:MainMenu />

            <controls:Segmented HorizontalAlignment="Right" SelectionMode="Single">
                <controls:SegmentedItem Icon="{ui:FontIcon Glyph=&#xE7C3;}" ToolTipService.ToolTip="Markdown" />
                <controls:SegmentedItem Icon="{ui:FontIcon Glyph=&#xE89A;}" ToolTipService.ToolTip="Split view" />
                <controls:SegmentedItem Icon="{ui:FontIcon Glyph=&#xE8FF;}" ToolTipService.ToolTip="Preview" />
            </controls:Segmented>

        </Grid>

        <Grid
            x:Name="Body"
            Grid.Row="1"
            Padding="0,0,12,0"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            ContextFlyout="{StaticResource MyContextMenu}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <RelativePanel Grid.Column="0">
                <TextBlock
                    x:Name="dir"
                    Padding="16,8,0,0"
                    Text="{x:Bind rootPath}" />
                <AutoSuggestBox
                    x:Name="Box"
                    Margin="12,8"
                    PlaceholderText="Search files..."
                    QueryIcon="Find"
                    RelativePanel.Below="dir" />
                <ScrollViewer
                    HorizontalScrollBarVisibility="Auto"
                    RelativePanel.Below="Box"
                    VerticalScrollBarVisibility="Auto">
                    <TreeView
                        x:Name="treeview"
                        Padding="8"
                        Collapsed="TreeView_Collapsed"
                        Expanding="TreeView_Expanding"
                        ItemInvoked="TreeView_ItemInvoked"
                        ItemTemplateSelector="{StaticResource ExplorerItemTemplateSelector}"
                        SelectionMode="Single" />
                </ScrollViewer>
            </RelativePanel>

            <controls:GridSplitter
                Grid.Column="1"
                Width="16"
                HorizontalAlignment="Left"
                ResizeBehavior="BasedOnAlignment"
                ResizeDirection="Auto">
                <controls:GridSplitter.RenderTransform>
                    <TranslateTransform X="-7" />
                </controls:GridSplitter.RenderTransform>
            </controls:GridSplitter>

            <Grid Grid.Column="1">
                <TabView
                    x:Name="Tabs"
                    VerticalAlignment="Stretch"
                    AddTabButtonClick="TabView_AddButtonClick"
                    SelectionChanged="Tabs_SelectionChanged"
                    TabCloseRequested="TabView_TabCloseRequested" />
            </Grid>
        </Grid>

        <Grid
            x:Name="Footer"
            Grid.Row="2"
            Padding="8,0"
            BorderThickness="0,1,0,0">
            <RelativePanel HorizontalAlignment="Left">
                <Button
                    Margin="8"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="Button_Click">
                    <FontIcon Glyph="&#xE713;" />
                </Button>
            </RelativePanel>
            <RelativePanel HorizontalAlignment="Right">
                <TextBlock
                    x:Name="WordCountText"
                    Padding="8"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="{x:Bind ViewModel.WordCount}" />
                <TextBlock
                    Padding="8"
                    Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                    RelativePanel.RightOf="WordCountText"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="words" />
            </RelativePanel>
        </Grid>
    </Grid>
</Page>
